# Shared anti-patterns

1. Embedding AGPL FreeMoCap into proprietary SaaS without legal review.
2. Duplicating movement schemas under `practice-relay/` and `mvei/`.
3. Breaking schema without dual-RFC.
4. Putting runtime show-control state as only source of truth (Practice Relay document owns identity).
5. Claiming work-record package invents packaging science (it is a domain profile).
